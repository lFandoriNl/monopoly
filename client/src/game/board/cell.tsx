type CellProps = {
  image: string;
  price?: number;
  priceColor: string;
  orientation: string;
  figure: string;
  position: string;
  type: string;
};

export const Cell = ({
  image,
  price,
  priceColor,
  orientation,
  figure,
  position,
  type,
}: CellProps) => {
  return;
};
